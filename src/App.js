import './App.css';
import MainApp from './app/MainApp';

function App() {
  return (
    <div style={{margin: 0, padding: 0, backgroundSize: 'cover', minHeight: '100vh' }}>
      <MainApp />
    </div>
  );
}

export default App;
